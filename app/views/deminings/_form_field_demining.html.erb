<div class="form-group select required ppsp_demining_id"><label class="select required" for="ppsp_demining_id">Centre de Déminage<abbr title="required">*</abbr></label>
  <select class="form-control select required" name="ppsp[demining_id]" id="ppsp_demining_id">
    <option value=""></option>
    <% @database.each do |data| %>
      <%= "<option value=#{data.id}>#{data.name}</option>".html_safe %>
    <% end %>
  </select>
</div>
<%= simple_form_for @demining, remote: true do |f| %>
  <%= f.input :name, label: 'Nom' %>
  <%= f.input :address, label: 'Adresse' %>
  <%= f.input :phone, label: 'Téléphone' %>
  <% if params[:action] == 'new' || params[:action] == 'index' %>
    <%= f.submit 'Créer un centre de déminage', class: 'btn-blue' %>
  <% else %>
    <%= f.submit 'Modifier ce centre de démninage', class: 'btn-blue' %>
  <% end %>
<% end %>
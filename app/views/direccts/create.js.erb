$(document).ready(function() {
  $(".modal-backdrop").hide();
  $("#ModalDirecct").hide();
  $(".modal-open").css({overflow: 'scroll'});
  if (document.getElementById("ppsp_direcct_id")) {
    document.getElementById("ppsp_direcct_id").innerHTML = "<%= j(render 'form_field_direcct') %>";
  }
  if (document.querySelector(".container-database")) {
    document.querySelector(".container-database").innerHTML = "<%= j(render 'list_direccts') %>";
  }
})


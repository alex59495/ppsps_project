<%= simple_form_for @project do |f| %>
  <%= f.input :reference %>
  <%= f.input :responsible %>
  <%= f.input :phone %>
  <%= f.input :email %>
  <%= f.input :site_manager_id, as: :select, collection: SiteManager.all.order(:name) %>
  <% if policy(SiteManager).new? %>
    <%= link_to "Ajouter un chef de chantier", new_project_information_path %>
  <% end %>
  <%= f.input :team_manager_id, as: :select, collection: TeamManager.all.order(:name) %>
  <% if policy(TeamManager).new? %>
    <%= link_to "Ajouter un chef d'équipe", new_project_information_path %>
  <% end %>
  <%= f.submit %>
<% end %>